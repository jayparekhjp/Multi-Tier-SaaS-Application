<% include partials/header %>
    <div class="container">
            <h2>Users List</h2>
            <ul>
            <% users.forEach(function(user){ %>
                <li><%= user.id %>    <%= user.name %></li>
            <%})%>
            </ul>
    </div>
<% include partials/footer %>